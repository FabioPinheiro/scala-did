services:
  demo:
    image: scala-did-sandbox
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/health >/dev/null"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 10s
