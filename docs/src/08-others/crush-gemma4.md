# Running Crush with Gemma 4

To run Crush using the Gemma 4 model from the command line, use the following command structure:

```bash
crush run --model ai/gemma4:E4B -C "<prompt>"
```

Replace `<prompt>` with your desired input query.